type Query {
	allUsers: [User!]!
	getUser(_id: ID!): User
}

type Mutation {
	createUser(data: UserInput!): User!
	updateUser(_id: ID!, data: UserInput!): User
}

type User {
	_id: ID!
	username: String!
	accounts: [String]!
}

input UserInput {
	username: String!
	accounts: [String]!
}
